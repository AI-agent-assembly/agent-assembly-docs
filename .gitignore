docs/book/

# Docs hub aggregation (AAASM-3659) — generated locally / in CI
/public/
/.modules/

# i18n (AAASM-4697): the PO catalog + template under docs/po/ are tracked, but
# the `mdbook build -d po` extraction scratch output is not.
docs/po/html/
docs/po/xgettext/
__pycache__/

# Content audit (AAASM-5603) — generated per run; CI stores it as a workflow
# artifact, not committed history, so it never needs a bot-commit back to main.
docs/audit-reports/
